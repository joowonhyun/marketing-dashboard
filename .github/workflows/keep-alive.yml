name: Render 백엔드 슬립 방지

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: /health 호출
        run: curl -sf https://marketing-dashboard-api-5k4k.onrender.com/health
